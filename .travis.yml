language: node_js
node_js:
  - "9"

before_install:
  - npm install -g gulp

cache: npm

script:
  - cd src
  - npm install
  - gulp build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  local_dir: /
  on:
    branch: master